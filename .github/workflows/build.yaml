name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
